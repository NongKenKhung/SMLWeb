flask==3.0.3
# WhisperX wraps OpenAI Whisper with batched inference (faster-whisper /
# ctranslate2) and word-level alignment via wav2vec2. Pulls in torch,
# torchaudio, transformers, ctranslate2, faster-whisper, pyannote.audio.
# Pinned to a non-yanked version (3.x.x lineage has lots of yanks; 3.8.5 is
# the current top of the un-yanked list).
whisperx==3.8.5
soundfile==0.12.1
